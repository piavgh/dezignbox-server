<nav class="navbar navbar-expand-md navbar-dark">
    <a class="navbar-brand" href="/">Dezignbox.vn</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item <%= req.path === '/start-designing' ? "active" : "" %>">
                <a class="nav-link" href="/start-designing"><%= __('startDesigning') %></a>
            </li>
            <% if (req.user) { %>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle"
                   href="#" id="navbarDropdown"
                   role="button"
                   data-toggle="dropdown"
                   aria-haspopup="true"
                   aria-expanded="false"
                >
                    <%= req.user.email %>
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="#"><%= __('products') %></a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/logout"><%= __('logout') %></a>
                </div>
            </li>
            <% } else { %>
            <li class="nav-item">
                <a class="nav-link" href="/"><%= __('aboutUs') %></a>
            </li>
            <li class="nav-item <%= req.path === '/register' ? "active" : "" %>">
                <a class="nav-link" href="/register"><%= __('register') %></a>
            </li>
            <li class="nav-item <%= req.path === '/login' ? "active" : "" %>">
                <a class="nav-link" href="/login"><%= __('login') %></a>
            </li>
            <% } %>
        </ul>
    </div>
</nav>